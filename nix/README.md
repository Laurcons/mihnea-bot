Just a basic nix config for the whitelist part of the bot.
# TODO @laurcons move the next to nix

$ nix profile install github:Laurcons/mihnea-bot/nix

For dev runs:

$ nix profile install <REPO_ROOT>/nix
